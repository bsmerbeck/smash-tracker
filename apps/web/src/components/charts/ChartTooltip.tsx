import type { TooltipContentProps } from 'recharts';

/**
 * RED-phase stub (plan 37-01 Task 2, tdd="true"): intentionally
 * unimplemented so the behavior tests below fail on real assertions before
 * the GREEN implementation lands. See the plan 37-01 SUMMARY for the
 * recorded RED evidence.
 */
export function ChartTooltip(props: TooltipContentProps<number, string>) {
  void props;
  return null;
}
