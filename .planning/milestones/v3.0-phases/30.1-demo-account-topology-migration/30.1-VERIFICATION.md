---
phase: 30.1-demo-account-topology-migration
verified: 2026-08-17T03:14:48Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: "Production re-verification after owner authorization"
behavior_unverified_items: []
human_verification: []
---

# Phase 30.1: Demo Account Topology & Migration — Verification Report

## Verdict

**PASSED — 6/6 must-haves verified.** Phase 30.1 produced four owner-controlled,
login-bearing demo accounts, copied each frozen Phase-30 dataset to the correct account UID,
made IzAw the root of its own coaching account, and reversibly archived the four source research
tenants. None of the represented players remains a managed client of the developer account.

This report is the authoritative verification record. It contains no superseded “step 10 open”
instructions. The source archive is complete and must not be repeated.

## Production chronology

1. The seven-tree migration completed on 2026-08-11.
2. All four source tenants were soft-archived on 2026-08-12.
3. Phase 30.2 later added destination-owned enrichment to the demo accounts.
4. A bounded, read-only production topology audit re-verified the archive and account topology on
   2026-08-16. No archive or content write occurred during re-verification.

The later instruction to archive before Phase 30.2 was based on stale chronology. Phase 30.2 Gate 2
was already complete. Never rerun that enrichment, copy it back to the sources, or invoke the legacy
archive command.

## Canonical account map and frozen-copy evidence

The migration artifact reported `ok: true`, production host
`smash-tracker-f97b7.firebaseio.com`, zero conflicts, zero assert-empty violations, and a match for
every source/destination tree. Its generated timestamp was `1786463913238`
(`2026-08-11T15:58:33.238Z`).

| Workspace | Source tenant | Destination UID | Matches | Opponents | Lossless source sets |
|---|---|---|---:|---:|---:|
| Hungrybox | `0f0443e4-c637-4dd4-9c3a-c8365c1f7ad6` | `B4AoA73kJ2dlk9B61POUsTUjM6w2` | 8,582 | 2,219 | 8,413 |
| MkLeo | `d4c17fdc-be36-49a1-8223-e0a3ab16a40c` | `eVJih9SgfJVk5oMPAQydPGbEBpU2` | 4,568 | 704 | 5,314 |
| Sparg0 | `e5d9f25d-929d-4884-875d-67cfab05d5c1` | `cosPe2wagVZsTWprGKnpjrcUEsb2` | 8,378 | 1,202 | 6,187 |
| IzAw | `1438981f-2830-469d-ac96-cca1ee8cc75b` | `VnWOqNRRP5ZqFF0457DPqiBsT4D3` | 237 | 48 | 640 |

Each account also copied one identity record, one terminal ingestion-run record, and one coverage
record. The source and destination counts matched for every listed tree. The source supplement
trees were empty at this frozen baseline.

Artifact custody identifiers:

- migration artifact file SHA-256:
  `919379610ca26868031ccb4b3de508c031188ec0d0eda7c3e2db35d710b33b88`
- migration artifact canonical/internal SHA-256:
  `ce09412e19d3440cf6680bda53bb5529ff286b2b42283d35ef484315850d6dbb`

The raw production migration artifact is intentionally retained outside the committed source tree.
The canonical map and every acceptance-relevant frozen count are transcribed above so this report
does not require that local file to interpret the pass. The hashes identify the retained raw artifact
if a future custody check is required.

## Production topology evidence

The read-only audit ran from reviewed commit
`9c026864e38a472a91213f820f5307e1e38a3be1`; exact-SHA GitHub Actions run `31985917813`
completed successfully before the production read. It targeted
`smash-tracker-f97b7.firebaseio.com`, observed no emulator, used developer coaching UID
`i7UM54MfCCSNusp85QeEKNQJ9fv2`, and audited the four exact destination UIDs in the map above.

| Workspace | `clientTenants.archivedAt` | `coachClients.archivedAt` | Visible in Client Hub |
|---|---:|---:|---|
| Hungrybox | `1786550993543` | `1786550993543` | No |
| MkLeo | `1786550993701` | `1786550993701` | No |
| Sparg0 | `1786550993842` | `1786550993842` | No |
| IzAw | `1786550993978` | `1786550993978` | No |

The source records were present, and both archive timestamps matched for every source. The audit
performed 28 bounded membership reads, found `visibleSourceTenantCount: 0`, returned `ok: true` and
`exitCode: 0`, and emitted no findings. It found neither of the two invalid topology classes:

- no destination UID was a member of a developer coach tenant;
- no destination UID owned a managed tenant through `clientOwnedTenants`.

The raw receipt's file SHA-256 is
`5acc84dd7bc271dfbdfde8bb64266e52fa4d7697c39a69d79c5f0a454d43302a`. It is retained as an
operator artifact outside the committed source tree. All acceptance-relevant observations from the
receipt are transcribed above.

## Must-have disposition

| # | Must-have | Result | Evidence |
|---|---|---|---|
| 1 | Four login-bearing accounts exist under their own UIDs; credentials remain owner-held and absent from source/planning/logs. | **PASS** | The exact four UIDs passed migration preflight, received copied records, and were used in the production topology audit. Only identifiers—not emails, passwords, or tokens—appear in the evidence. |
| 2 | The frozen Phase-30 dataset was copied without overwrite, conflict, loss, or cross-account substitution. | **PASS** | Manifest `ok: true`; exact named source→destination map; all seven source/destination tree counts matched; zero conflicts and zero assert-empty violations; canonical hashes recorded above. |
| 3 | The source workspaces were preserved and reversibly disabled, never deleted. | **PASS** | All eight metadata paths remained present with matching non-null archive timestamps. The archive implementation changes metadata only; no delete was authorized or performed. |
| 4 | IzAw is its own coaching ownership root, not a client of the developer. | **PASS** | IzAw's coaching mode was enabled by the migration's silent field-scoped operation; the production audit found no destination membership or managed ownership under the developer tree. Fictional client content remains Phase 33. |
| 5 | The four destinations are data-audit-ready and their coverage is honest. | **PASS** | Each destination received its terminal ingestion state and coverage record; the Phase-30 audit had 12/12 tournament spot-checks with zero delta, and each login rendered its populated coverage. Missing provider facts remain explicit. |
| 6 | The account topology matches the owner correction: represented players do not appear in the developer Client Hub. | **PASS** | Four archived source rows, zero visible source tenants, zero destination membership/ownership findings. |

Requirements closed by this verification: **ACCT-01, ACCT-02, ACCT-03, WKSP-01A, and VAL-01**.

## Evidence boundary and standing constraints

The topology audit proves metadata presence, archive state, Client Hub invisibility, and destination
membership/ownership. It does **not** traverse or hash every source-content tree. Source-content
preservation is supported separately by the clean-copy manifest and by the field-only soft-archive
implementation. This boundary must remain explicit; the topology receipt alone is not a content
backup.

The original comparator now correctly refuses because later destination-only enrichment has evolved
the demo accounts beyond the seven-tree frozen copy. That refusal is not migration failure. It is why
the legacy archive/copy path must never be replayed against the evolved destinations.

Phase 30.1 is complete. Phase 31 remains gated only by the remaining Phase 30.2/30.3 verification
refresh and Gate 6 acceptance—not by account creation, migration, topology, or source archival.

---

_Verified: 2026-08-16T23:14:48.370-04:00_
_Verifier posture: exact-SHA CI first, then bounded production read-only audit; zero production writes_
