---
gsd_state_version: 1.0
milestone: v3.0
milestone_name: Pro-Readiness — Evidence-First Stats & Research Workspaces
current_phase: 30.3
current_phase_name: Demo Usability Closeout (Gate 6 production UAT)
status: executing
stopped_at: "2026-08-16 SIXTH CORRECTION (pass 8 of the checklist) — Codex hard-gate re-review at 6561a471 confirmed exact-SHA CI 31974881638 green and the application/package/Dockerfile/Firebase/lockfile trees byte-identical to the reviewed code, then returned ADJUST with a P1 SAFETY BLOCKER plus three items, all docs/operator-only. (1) BLOCKER, effective API environment was never validated: the preflight PARSED apps/api/.env, but P3-P7 resolve their environment via process.loadEnvFile('.env') + loadEnv(), and loadEnvFile does NOT overwrite an already-set variable — so an inherited FIREBASE_DATABASE_URL beat the file for the operators while the preflight, reading the file, reported the production host and exited 0. REPRODUCED INDEPENDENTLY here on Node v24.11.1 (both the loadEnvFile non-overwrite rule and the old preflight printing OK/exit 0 in a shell whose effective value was https://totally-wrong-project.firebaseio.com) — that put P3 reads and P4 WRITES on the wrong RTDB before P5's database binding could catch it. Fixed with new Stage A2 (unset FIREBASE_DATABASE_URL FIREBASE_DATABASE_EMULATOR_HOST, kept for the whole P1-P7 session) and Stage A5b (a preflight that refuses inherited shadowing vars, then walks the operators' own loadEnvFile + loadEnv path and asserts the EFFECTIVE host is exactly smash-tracker-f97b7.firebaseio.com with no emulator); executed three ways — inherited wrong URL exit 1, inherited emulator host exit 1, sanitized exit 0. (2) P1 split into Stage A (no production contact: SHA pin, sanitization, frozen install, BOTH env transfers, both effective-env preflights, worktree-clean check, web build + prerender, API image build+push) and Stage B (the only mutating half: one API deploy, traffic pin+assert, one hosting deploy) — previously the API deploy and traffic change ran BEFORE dependency installation, env transfer, and the web build. (3) the traffic "MACHINE-CHECK" was output-only and exited 0 on a split; Stage B2 now reads the created revision from the deploy record independently, requires created == ready, routes THAT revision by name to 100% (not --to-latest, a moving target), parses status.traffic[] from --format=json, and exits nonzero unless exactly one serving entry names it at 100% — falsified across 8 synthetic cases (good / 50-50 split / wrong revision / 90-10 / 0%-tag alongside / empty / non-JSON / empty expected). (4) cleanups: the api-origin-headers-missing diagnostic no longer references an undefined $TOKEN or a placeholder Cloud Run URL (derives status.url, prompts a fresh hidden token via the zsh read form, unsets after), and STATE.md's live "Current focus" field — still reading Phase 30.2 — is corrected to 30.3. Also committed: assert-p1-ordering.mjs beside the checklist (same reviewed-gate exception), which parses P1's fenced code blocks and asserts every local prerequisite precedes the first gcloud run deploy / update-traffic / firebase deploy; it PASSES on the corrected file and FAILS (exit 1) on the previous committed revision 1e086b38 — a proven falsifying case, and it FATALs rather than passing vacuously if it finds no mutating command at all. Docs-only: no source touched, no production or registry contact, no token value written anywhere. GATE 6 REMAINS BLOCKED pending Codex review of pass 8 and its actual execution. 30.1 step-10 archive sign-off STILL separately open, unchanged. Prior entry (superseded by the sixth-correction work above, kept for detail): "2026-08-16 FIFTH CORRECTION (pass 7 of the checklist) — Codex hard-gate re-review at 04da04ed confirmed the runbook is committed and CI-green, returned ADJUST with 7 items, 4 docs-only/mine: (1) P1 was doing TWO API deployments (a --source deploy AND a separate --image deploy — not reproducible); replaced with exactly one `docker buildx build --platform linux/amd64 --build-arg API_RELEASE_SHA=... --push` + one `gcloud run deploy --image`, plus a hosting half that transfers the gitignored apps/web/.env.production into the clean worktree, asserts all four required VITE_FIREBASE_* vars are non-empty with a RUNNABLE check (the validation is otherwise lazy — build succeeds, app breaks at runtime), runs the real `build`+`prerender` scripts (not invented ones — checked apps/web/package.json), and deploys with `firebase deploy --project smash-tracker-f97b7 --only hosting` pinned. (2/3) LANDED and merged at 713b5742 (CI 31973418830 green): --expected-api-release-sha is now a SEPARATE mandatory flag alongside --expected-api-revision (the old observedBuild = revision ?? releaseSha was a proven false green — RED->GREEN reproduced independently: against 04da04ed a capture with the right revision and a WRONG release SHA exited 0 and sealed a probe, because Cloud Run always injects K_REVISION so the SHA was never consulted and the API_RELEASE_SHA build arg was decorative); mixed-revision probes are blocked by x-gf-api-revision/x-gf-api-release-sha response headers emitted on an onSend hook, gated on request.uid so anonymous and public-bearer surfaces stay byte-identical, validated across all three probe requests and re-derived by the audit; the audit's status === statusCode self-comparison now reasserts the literal 403. Probe format 2->3, audit receipt 4->5, old artifacts refused by name. (4) deleted the 'lighter P5 posture' opt-out outright from P5 and P7 — all four accounts + --require-rejected-operation-probe are now unconditional, no partial-coverage form of the gate exists. (5) picked ONE fix for the 6h-manifest/24h-receipt-vs-unbounded-manual-UAT hazard: reordered so P4 (all four accounts) through P7 run as one continuous CLI-only session with NO manual browser UAT interleaved (moved Section S's walkthrough to strictly AFTER P7 passes, removed the old 'do not advance past sparg0 until Section S passes' gate), stated explicitly in a new callout above P1 and a transition note after P7; P6 stays as defensive insurance only, with an explicit note that refreshing receipts does NOT repair an expired manifest (regenerate+re-review those instead). (6) replaced the inline `GATE6_DEMO_ID_TOKEN='<token>' pnpm ...` form (records the token in shell history) with a `read -rs -p ...` no-echo prompt, `export` without the value ever on a command line, and an `unset` immediately after each capture. Verified via `apps/web/src/lib/firebase.ts` (exact required var names), `apps/web/package.json`/root `package.json` (real script names, no invented ones), `.firebaserc` (project id), and `.gitignore` (.env.production confirmed gitignored) — did not assume any of it. Applied docs-only to 30.3-UAT-CHECKLIST.md (now pass 7; top banner + all downstream 'P1 Prerequisite A/B' references updated for consistency). No source touched, no production contact, no token value written anywhere. GATE 6 REMAINS BLOCKED pending owner/Codex review of the corrected runbook and its actual execution. 30.1 step-10 archive sign-off STILL separately open, unchanged." Prior entry (superseded by the fifth-correction work above, kept for detail): "2026-08-16 FOURTH CORRECTION — RUNBOOK NOW COMMITTED, SINGLE AUTHORITATIVE SOURCE. Codex hard-gate follow-up: (1) landed the deployment-identity endpoint + stable demo-checkout refusal code + capture-operator environment binding at HEAD 5e2ebb18 (probe format 2, gate6Audit's own receipt format 4); (2) is force-adding .planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md to git after this pass (a reviewed-gate exception to the general .planning/ gitignore, same precedent as 30.2-13-SUMMARY.md) because the reviewed SHA's committed tree previously did not contain it, so it could not be cited as reproducible evidence. Re-derived P1/G7/P5/the disagreement box against 5e2ebb18 by reading gate6CaptureProbeCore.ts, gate6CaptureProbe.ts, apps/api/src/routes/deploymentIdentity.ts, apps/api/src/config/env.ts, packages/shared/src/deployment.ts + error.ts, and Dockerfile directly — did not trust the coordinator's summary. Key additions: P5's capture command now requires two new no-default flags (--expected-api-revision, --expected-api-environment; the revision value must be the Cloud Run revision name, since Cloud Run always injects K_REVISION and the operator prefers it over the baked-in release SHA) and now BINDS the deployed API to the local RTDB before snapshotting (GET /api/deployment-identity, same bearer token, aborts on environment/revision/database/emulator/project mismatch); the refusal proof now requires the exact application code demo_account_checkout_forbidden, not just a 403; G7 gained a second check (deployment-identity 200 with revision/releaseSha present, feeding $EXPECTED_API_REVISION into P5); P1 gained two explicit deploy prerequisites — redeploy from a build carrying both new server features, AND bake API_RELEASE_SHA into the image, which required documenting a build+push+deploy-by-image workaround because `gcloud run deploy --source .` (this project's existing documented deploy command) has NO flag for passing a Docker build-arg through — VERIFIED via `gcloud run deploy --help` and external research (Google's own build-arg guidance), not assumed. The full re-derived 9-item disagreement box (now 15 items) lives inline in the checklist. THE CHECKLIST FILE IS NOW THE SINGLE AUTHORITATIVE SOURCE for Gate 6 execution — every other planning artifact (this STATE.md summary included) should point to it rather than restate its commands; an operator with only that committed file and repo access can execute Gate 6 without any chat-history reconstruction. Docs-only, no source touched beyond reading, no production contact, no token value written anywhere. GATE 6 REMAINS BLOCKED pending owner/Codex review of the committed runbook and its actual execution. 30.1 step-10 archive sign-off STILL separately open, unchanged." Prior entry (superseded by the fourth-correction work above, kept for detail): "2026-08-16 THIRD CORRECTION (probe-capture operator landed) — the coordinator reported that apps/api/scripts/gate6CaptureProbe.ts + gate6CaptureProbeCore.ts (the tool P5's second-correction fallback was written around not having) merged at 637a0a39, about a minute after the second correction checked and found it absent. HEAD is now 637a0a39; re-derived P5/P7 and the disagreement box against it directly (read gate6CaptureProbe.ts and gate6CaptureProbeCore.ts myself, did not trust the coordinator's example flags verbatim — they matched). P5 is now the REAL capture step: run gate6CaptureProbe.ts once per account (four sign-ins), credential supplied ONLY via GATE6_DEMO_ID_TOKEN env var (never a flag — shell history/ps risk), obtained by copying the Bearer value from an authed /api/... request in browser DevTools after signing in as that demo account, ID tokens expire ~1h so capture+audit must happen in one sitting. The refused operation is hardcoded as POST /billing/checkout (real pack id, expects EXACTLY 403) — 2xx aborts as a production-defect finding (refused-operation-succeeded, stop-ship, not a capture bug), any non-403 status aborts as refused-operation-unexpected-status (including 401, which would otherwise pass vacuously), a UTC-midnight-straddling call aborts harmlessly and just needs a re-run. --api-base-url must include the /api segment (Fastify prefix + Hosting rewrite) — https://grandfinals.gg/api, not the bare origin. New explicit OWNER DECISION documented in P5: one probe is enough to make rejected-operation-no-trace CHECKED, all four is the stronger posture and is what lets --require-rejected-operation-probe pass — this runbook defaults to all four, required. P7's skippedCount: 0 is now stated as flatly required, no hedge/exception — capturing landed, so every input P7 needs genuinely exists. Disagreement-box item about the capture CLI not having landed is REMOVED; the other 8 items re-verified line-by-line against 637a0a39 (gate6AuditCore.ts shifted ~15 lines from a monitor-extraction refactor that changed no assertion logic — createGate6Monitor now exported and shared with the new capture operator; citations updated: receipt.command check now at :1913, registry-manifest-missing at ~2004/2014). G6's named tip-of-master moved from c936bc39 to 637a0a39 (this pass did not itself re-run the full suite against it — G5 must still be independently reconfirmed before P1, docs-only scope). Applied docs-only to 30.3-UAT-CHECKLIST.md — no source touched, no production contact, no token value written anywhere. GATE 6 REMAINS BLOCKED pending owner/Codex review of this now-fully-executable runbook and its actual execution. 30.1 step-10 archive sign-off STILL separately open, unchanged." Prior entry (superseded by the third-correction work above, kept for detail): "2026-08-16 SECOND CORRECTION — a fresh independent verifier re-audited the first-correction runbook against HEAD c936bc39 and returned NOT READY: the terminal audit step (old P6) could not exit 0 as written, NO PRODUCTION MUTATION HAS OCCURRED. Concrete defects fixed, all re-derived directly against c936bc39 (not trusted from the prior pass): (1) the final audit omitted --registry-manifest, which gate6AuditCore.ts now REQUIRES alongside every --registry-receipt (its absence is a registry-manifest-missing finding per receipt, gate6AuditCore.ts ~1986-1999, locked by gate6AuditCore.test.ts:2097) — fixed by carrying all four dry-run manifest paths (already produced earlier in the per-account backfill) into the final audit, unchanged. (2) skippedCount: 0 was unsatisfiable: a NEW assertion, rejected-operation-no-trace, skips without --rejected-operation-probe, and as of c936bc39 there is still no landed CLI script producing one — only read-only library primitives (captureGate6TraceSnapshot/createGate6RejectedOperationProbe) exist inside gate6AuditCore.ts, confirmed by listing apps/api/scripts/. Fixed with a new pre-audit step that checks for a landed capture operator via source (never invents flags) and uses it if present; otherwise the final audit runs without probe evidence and that ONE assertion is allowed to report status: skipped (skippedCount: 1, named explicitly at sign-off) — every other assertion must still show passed. (3) the first correction's disagreement-box item 5 was FACTUALLY WRONG — gate6AuditCore.ts:1898 rejects everything except a compare receipt (apply receipts are explicitly refused too, not accepted alongside compare as previously claimed); the whole disagreement box was rewritten fresh, which also caught that --account is NOW mandatory for apply (deriveTournamentRegistryCore.ts, landed since the first pass). (4) the old read-only witness-assertion step had no executable command and was ordered before the UID-export step — folded into the baseline-record step, which already asserts the same mkleo/sparg0 counts via its own unconditional expected-counts assertion; --strict-witness-observation-refs moved into that same early step so a dangling witness reference surfaces pre-write, not after all four accounts are backfilled. (5) two staleness time bombs — registry-receipt freshness 24h (gate6AuditCore.ts, --max-receipt-age-ms) and registry-manifest freshness 6h (deriveTournamentRegistryCore.ts, --max-age-ms, unchanged) — get an explicit new pre-final-audit step: refresh all four compare receipts immediately before the final run, since the Supernova 2026 manual browser walkthrough (gated behind sparg0's backfill) can span real time. (6) new gate: verify the DEPLOYED API actually carries DEMO_ACCOUNT_UIDS (sign in as a demo account, confirm GET /api/users/me returns isDemoAccount: true) before trusting ANY of the account-login UAT items — an unset/misspelled var makes isDemoAccountSubject return false for every uid with no error (env.ts/demoAccount.ts). (7) the deploy gate's 'final reviewed SHA' is re-pointed from e4c76921 (RED apps/web build-mode typecheck) to c936bc39 (the fix commit); the suite-green gate now specifies typecheck must run as root `pnpm typecheck` (`tsc -b --noEmit` for web, project-references BUILD mode) since a bare `tsc --noEmit` is a different, weaker program that produced a false green on e4c76921. Applied docs-only to `.planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md` (full re-derived 'flag for lead' box inline after the P section) — no source touched, no production contact. GATE 6 REMAINS BLOCKED pending owner/Codex review of THIS twice-corrected runbook and its actual execution. 30.1 step-10 archive sign-off STILL separately open, unchanged." Prior entry (superseded by the second-correction work above, kept for detail): "2026-08-16 CODEX HARD GATE 4 — ADJUST received on the Gate 6 production runbook, corrective work IN PROGRESS, NO PRODUCTION MUTATION HAS OCCURRED. Codex's exact finding: the P-section registry-backfill sequence written for Gate 6 UAT was IMPOSSIBLE to execute — (1) `--require-baseline` refuses to run in RECORD mode by design, so a fresh baseline can never be recorded with it set; (2) `--require-registry-receipt` requires ALL FOUR accounts' receipts, so a single account's receipt can never satisfy it; (3) wildcard receipt paths (`registry-receipt-apply-X-*.json`) are ambiguous after any retry because the filename embeds a fresh per-invocation timestamp. Corrective runbook written and applied to `.planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md` §P (docs-only, read the actual current CLI surfaces in `gate6Audit.ts`/`gate6AuditCore.ts`/`deriveTournamentRegistry.ts`/`deriveTournamentRegistryCore.ts` before writing it): ONE four-account baseline recorded WITHOUT `--require-baseline` before any apply; per-account scoped dry-run → owner review → scoped apply → scoped compare, gated on the operator's own compare exit code and the EXACT printed `[receipt] <ws>: status=<s> path=<p>` path (never a glob); ONE final audit after all four accounts pass, using the baseline + `--require-baseline` + all four exact compare-receipt paths + `--require-registry-receipt` + `--strict-witness-observation-refs`, zero skipped assertions required. Also recorded: `deriveTournamentRegistry.ts apply` does not yet hard-require `--account` in the code as of this writing (a parallel agent is adding that), and the corrected runbook already never omits it either way; deploy step reinforced as a clean DETACHED worktree at the reviewed SHA (G6/P1). Full "flag for lead" disagreement list lives inline in the UAT checklist right after the P section. Production state UNCHANGED from the prior entry below — registry backfill NOT yet applied, no accounts touched. NEXT: owner/Codex review of the corrected runbook, then execute Gate 6 P1-P6 for real. GATE 6 REMAINS BLOCKED pending that review and execution. 30.1 step-10 archive sign-off STILL separately open, unchanged." Prior entry (superseded by the corrective work above, kept for the production-state facts it still carries): "2026-08-16 CORRECTIVE COMPLETE — independent verifier FINAL VERDICT: READY FOR GATE 6 UAT (master f04b4887, CI green; tri-suite shared 901 / api 3384 / web 2431 / acceptance 89, run by the verifier itself). Gate 5 merged+integrated (fused witness-idempotent projection design, character/stock evidence, VOD normalization, config-gated YouTube candidates), verifier blockers B1 (web/shared attribution nullability chunk-wipe) and B2 (witness-delta reconciliation) fixed and re-verified 'genuinely closed'; B3 (candidate strip) fixed ahead of any YOUTUBE_API_KEY. Production character stamp executed: reconciled 24+26 sets; witnesses now 120/120 (mkleo) and 130/130 (sparg0) seat-proven with characters, stocks 70/76; exact totals unchanged (9,367/57, 10,335/68). Full record: 30.2-GATE2-AUDIT.md (+3 addenda) and HANDOVER-2026-08-16-corrective-complete-gate6-ready.md (now SUPERSEDED — see HANDOVER-2026-08-16-hard-gate-4-runbook-corrected.md). NEXT = GATE 6 (owner): deploy → registry backfill (dry-run→apply→compare) → probes → codex-uat → owner walkthrough (Supernova 2026 Sparg0-vs-Tweek acceptance fixture) → Codex hard gate 4 → flip verifications → Phase 31 unblocks. 30.1 step-10 archive sign-off STILL separately open." Gate 1 (reliability, merge after 60789e47): parity seam prepareAndValidateObservation at all gather sites, observationPersistenceHash in the write-set hash, manifest formatVersion 2 rejecting all v1 manifests BY NAME, lifecycle-safe operator (finally-owned teardown, heartbeats, stall watchdog, 10s exit backstop, child-process proofs), account-scoped status/dry-run/preflight/apply/resume/compare with lease renewal + durable cursors + preview-triggered reconciliation; adapters never emit empty rawTag (extraction-failure records without players; legacy reset-inheritance fabrication also fixed); whitespace rawTag structurally unpersistable (tightened, relax advisory superseded). Gate 3 (registry) + Gate 4 (web) merged earlier with a lead integration pass (60789e47) that fixed the cross-agent startggEventId string/number contract defect (pinned by historicalTournamentContract.test.ts) and added the 409 prep-activate guard on admin-imported rows. Tri-suite on master: shared 831 / api 3249+1skip / web 2389, tsc ×2 + lint clean (one non-reproducing first-run api flake, watch CI). Evidence/VOD agent (Gate 5) dispatched with the stage-witness idempotency hazard as mandatory first fix. NEXT: push → CI green → fresh v2 dry-run manifest → Codex pre-apply gate → owner-run serialized Gate 2 apply (MkLeo, Sparg0, IzAw; hbox completed no-op). 30.1 step-10 archive sign-off unchanged, separately open."
last_updated: "2026-08-16T20:15:00.000Z"
last_activity: 2026-08-16
last_activity_desc: "Autonomous run: Phase 30 re-verified PASSED 51/51 (stale human_needed artifact superseded from owner evidence recorded during 30.1 Wave 2; Codex advisor approved), ROADMAP 8/8, milestone 2/9 verification-passed. Phase 30.1 verification artifact created for the first time: human_needed 3/6, with three open items and two operational warnings that change what step 10 means (registry drift 7->15 copy trees will make archive refuse once 30.2 lands; migration-manifest.json is untracked and unignored). New confirmed defect claim: the no-managed-client clause in ACCT-01/ACCT-03 has no executed check. Loop HALTED at 30.1 - every remaining gate is owner-only or production. NO production mutation."
progress:
  total_phases: 9
  completed_phases: 2
  total_plans: 38
  completed_plans: 38
  percent: 22
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-07)

**Core value:** From a tournament, opponent, or coaching workspace, produce a prioritized evidence-linked practice plan faster and more reliably than the user can assemble manually.
**Current focus:** Phase 30.3 — Demo Usability Closeout, Gate 6 production UAT (blocked on Codex hard-gate review of the corrected runbook and the owner-run P1 deploy)

## Current Position

Phase: 30.3 Gate 6 — production UAT, BLOCKED on (a) Codex hard-gate review of `6561a471` and (b) the owner-run P1 deploy. **PROBED 2026-08-16: production runs `smash-tracker-api-00080-4ch` (2026-08-15) at 100% traffic and `GET /api/deployment-identity` 404s there — none of the Gate 6 code is deployed. Hosting's served bundle matches local dist but BOTH are stale.** Read `.planning/HANDOVER-2026-08-16-gate6-awaiting-codex-and-deploy.md` first, then the committed runbook `.planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md` (single authoritative source — do not reconstruct commands from any handover). Phase 30.1 step-10 archive sign-off is a separate, still-open owner gate.
Plan: 30.1-04 steps 1-9 COMPLETE (copy verified in prod, manifest `ok=true` hash `ce09412e…d6dbb`) — archive stays gated on separate explicit owner sign-off. 30.2 corrective COMPLETE (independent verifier READY-FOR-GATE-6 verdict at `f04b4887`; see prior `stopped_at` entry for full detail). 30.3 Gate 6 (production UAT): **the runbook is `.planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md` — READ IT DIRECTLY, it is now the single authoritative source and this STATE.md summary does not restate its commands.** G1-G5 code-side criteria previously green. **The runbook IS COMMITTED** (force-added `05ad234f` as a reviewed-gate exception to the general `.planning/` gitignore; verified present in a clean detached worktree with zero dirty files) and has been amended by later commits — read the tip with `git rev-parse HEAD`, never a SHA transcribed in a planning doc.

**Do NOT cite `gsd-tools verify phase-completeness 30.3` as evidence of completion.** It reports `complete: true` alongside `plan_count=0` and `summary_count=0` — i.e. it is confirming that a phase with no plans has no incomplete plans. That is a vacuous pass, the same structural failure as an audit that exits zero by default, and it was mistakenly cited as supporting evidence once (2026-08-16). Gate 6 completion is established ONLY by: the committed runbook's own gates, the `gate6Audit.ts` JSON receipt exiting 0 with `skippedCount: 0`, an independent-verifier verdict, a Codex hard-gate pass, and actual owner UAT. Phase 31 stays blocked until those land, and ACCT-02 / WKSP-01A remain explicitly unresolved (see `phases/30.1-.../30.1-ARCHIVE-DECISION.md`).
Status: SIXTH CORRECTION APPLIED (2026-08-16, checklist pass 8) — effective-API-environment validation (the P1 safety blocker: `loadEnvFile` never overwrites an inherited var, so a shadowed `FIREBASE_DATABASE_URL` would have put P4 writes on the wrong RTDB while the old file-parsing preflight reported production and exited 0), P1 split into a no-production-contact Stage A and a mutating Stage B so every local prerequisite and both deployable artifacts precede the first deploy, and a traffic assertion that actually fails on a split. Pass 7 previously delivered — one reproducible API deployment (was two), a runnable pre-build env-var assertion for hosting, an unconditional four-account gate, P4-P7 reordered ahead of manual UAT to protect the manifest/receipt freshness windows, and token capture off shell history. See `stopped_at` above for the full breakdown across all corrections; full detail lives in the checklist itself, the single authoritative source. Zero production contact was made producing any correction; no token value was written anywhere. Gate 6 remains BLOCKED pending owner/Codex review and actual execution. 30.1 archive sign-off: separate, unchanged. Phase 31 blocked until Gate 6 passes.
Progress: [████░░░░░░░░░░░░░░░░] 2/9 phases verification-passed (29, and **30 as of 2026-08-16** — see below). **`completed_plans: 38 / total_plans: 38` in the frontmatter means every WRITTEN plan has a summary — it does NOT mean the milestone is done.** Phase 30.3 carries ZERO plans by design (its work is the committed Gate 6 runbook, not a plan set), which is also why `gsd-tools verify phase-completeness 30.3` reports complete:true with plan_count=0 — a vacuous pass, not evidence. Real remaining work: Gate 6 production UAT (owner), then 30.1 step-10 archive sign-off, then ACCT-02/WKSP-01A, then Phase 31.
Last activity: 2026-08-16 — Sixth correction pass (checklist pass 8) absorbed: closed the P1 safety blocker by validating the EFFECTIVE api environment through the operators' own `process.loadEnvFile` + `loadEnv` path and rejecting inherited shadowing vars, split P1 into a no-production-contact Stage A and a mutating Stage B so every local prerequisite and both deployable artifacts precede the first deploy, replaced the output-only traffic "MACHINE-CHECK" with a parsing assertion that fails on a split, and fixed the `$TOKEN`/placeholder-URL diagnostic. Docs-only, no production contact, no token value written anywhere.

## Performance Metrics

**Velocity:**

- Total plans completed (prior milestones): v1.0: 16, v1.1: 27, v2.0: 32, v2.1: 6, v2.2: 14, v2.3: 2, v2.4: 14, v2.5: 31
- Average duration: - min
- Total execution time: - hours

**By Phase (v3.0 — in flight):**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 29 | 12 | - | - |
| 30 | TBD | - | - |
| 31 | TBD | - | - |
| 32 | TBD | - | - |
| 33 | TBD | - | - |
| 34 | TBD | - | - |

**By Phase (v2.5 — SHIPPED 2026-08-06):**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 26 | 10/10 ✓ | - | - |
| 27 | 11/11 ✓ | - | - |
| 28 | 10/10 ✓ | - | - |

**By Phase (v2.4 — SHIPPED 2026-07-28, archived):**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 23 | 6/6 ✓ | - | - |
| 24 | 7/7 ✓ (owner-validated 2026-07-28) | ~4h | ~34 min |
| 25 | 1/1 ✓ | - | - |

**Recent Trend:**

- Last 5 plans: -
- Trend: -

*Updated after each plan completion*

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- **AUTHORITATIVE OWNER CORRECTION (2026-08-09):** v3.0 creates four actual login-bearing demo accounts through the existing fake-email/password registration path. Hungrybox, MkLeo, and Sparg0 are ordinary player accounts containing their own imported histories. IzAw is a login-bearing player-and-coach account containing IzAw's own history plus fictional coaching clients. The developer account is not the coach/ownership root for these four accounts. This supersedes the earlier `no player-named auth principals`, non-auth research-subject, non-auth IzAw portfolio, and pre-demo OAuth-claim assumptions. The current managed-client rows do not satisfy WKSP-01A/WKSP-01B. Preserve the lossless ingestion substrate; pause live UAT and Phase 31; insert/replan identity topology and non-destructive migration first. Canonical record: `OWNER-CORRECTION-DEMO-ACCOUNTS.md`.
- **Correction reconciliation COMPLETE + owner-refined; Phase 30.1 boundary confirmed (2026-08-09):** GSD correction pass done and the owner's four approval-time scope refinements applied — Phase 30.1 (Demo Account Topology & Migration) inserted; REQUIREMENTS.md + ROADMAP.md rewritten (added ACCT-01/02/03; WKSP-01A remapped Phase 30 → 30.1; RTEN-01/07 superseded-as-deliverable with Phase-29 substrate preserved; RTEN-02/03/04/05 + PROOF-01/02 + WKSP-01B re-scoped to demo accounts; Phase 33 repointed to the logged-in IzAw account). **Owner refinements:** (1) Phase 30.1 proves the IzAw coaching-ownership topology ONLY — portfolio content stays Phase 33/PROOF-01; (2) custom demo-to-permanent CONVERSION deferred out of v3.0 — PILOT-02/03/04/06 deferred, opt-in uses the existing account-creation flow; (3) Phase 34 kept = PILOT-01 privacy + PILOT-05 retention/zero-orphan + WKSP-01B readiness + VAL-04 feedback + VAL-05 owner walkthrough; (4) ACCT-02 = idempotent non-overwriting copy→compare→validate→sign-off→archive/disable manifest, NO permanent deletion during 30.1. Coverage re-run + adversarially audited twice: **52 active-mapped + 4 deferred = 56 defined, zero orphans/duplicates/phantom IDs**; all audit-found discrepancies fixed. NO accounts created, NO data migrated. Migration surface preserved untouched in prod RTDB (4 research tenants w/ partial data). Next: on final owner OK, `/gsd-plan-phase 30.1`.

- Roadmap (v3.0, REVISED 2026-08-07 after owner review): 52 requirements across 6 phases, numbering continued from v2.5's Phase 28 — Research Tenancy, Isolation & Governance Gate (29) → start.gg Lossless Ingestion & Four-Player Coverage Audit (30) → Evidence Engine, Evaluation Protocol Freeze & Scale/Sparse Fixtures (31) → Evidence-Grounded Reports, Bracket Prep & Executed Calibration (32) → Coaching Proof: Izaw Portfolio & Full Coach Loop (33) → Verified Pilot Claims, Workspace Readiness & Owner Walkthrough (34). Strict numeric order; every arrow is a hard dependency, not a preference. Supersedes the earlier 5-phase/49-requirement draft.
- Roadmap (v3.0): per-phase requirement totals — 29:10 (RTEN-01..07, GOV-01, GOV-02, PROOF-03) · 30:10 (ING-01..08, VAL-01, WKSP-01A) · 31:10 (EVID-01..06, RPT-08, VAL-02A, FIXT-01, FIXT-02) · 32:11 (RPT-05..07, RPT-09, RPT-10, EVID-07..09, VAL-02B, VAL-03, WKSP-02) · 33:2 (PROOF-01, PROOF-02) · 34:9 (PILOT-01..06, WKSP-01B, VAL-04, VAL-05). Machine-verified 52/52 mapped, zero orphans, zero duplicates.
- Roadmap (v3.0): **VAL-02A/VAL-02B is a protocol-freeze-then-execute pair, deliberately split across the 31→32 boundary.** VAL-02A (Phase 31) freezes the backtesting protocol — historical pre-outcome snapshots or masked reconstructions, leakage protections, a named baseline, fixed encounter-band definitions, pre-declared stop-ship thresholds — BEFORE the bracket engine exists, so the engine cannot define the test it must pass or have thresholds relaxed to fit its results. VAL-02B (Phase 32) executes the completed EVID-07/EVID-08 engine against that frozen protocol; top-k opponent recall AND encounter-band calibration must pass before Phase 32 can complete. A failing calibration stops the phase, it never reopens the protocol. Standing boundary: encounter-band calibration validates encounter likelihood only and must never be cited as validating win probability (BRKT-02 stays v2).
- Roadmap (v3.0): **WKSP-01A/WKSP-01B split.** WKSP-01A (Phase 30) = four workspaces provisioned, backfilled, data-audit-ready with shortfalls visible. WKSP-01B (Phase 34) = all four fully walkthrough-ready across intended player/coach workflows. Phase 30 structurally cannot claim walkthrough-readiness because the evidence surfaces (31), reports/prep (32), and coach loop (33) do not exist yet for a walkthrough to traverse.
- Roadmap (v3.0): **RTEN-03 is narrow on purpose.** Phase 29's criterion closes PUBLIC and BEARER-TOKEN delivery routes (share links, /s/:token, /r/:token, OG cards, recaps, exports) fail-closed against research tenant IDs, proven by structural-absence tests — and deliberately leaves the authenticated internal/as-client preview permitted. That seam is what Phase 33's PROOF-02 coach-loop demonstration rides; a blanket delivery rejection would make the coaching proof unbuildable. Never write the criterion as "all delivery rejected".
- Roadmap (v3.0): **EVID-02 now carries cohort composition** ("comparable enough to act on"). Beyond sample size/denominator/coverage/date-range/refresh/policy-version/recency/confidence, every aggregate exposes its cohort composition and refuses to silently pool materially incomparable evidence — online/offline/unknown context, data source, confirmed-versus-missing character coverage, and set format/ruleset are distinguished. This is enforceable only because ING-03's lossless source layer (Phase 30) preserves those fields; the legacy-match projection drops them.
- Roadmap (v3.0): **PILOT-06 is a DISTINCT coach-portfolio claim transition** (Phase 34), not a reuse of the v2.4 single-client flip. The claimant verifies via confirmed primary start.gg player identity; one atomic idempotent operation transfers the non-auth portfolio subject PLUS all client memberships and reverse indexes together; no portfolio data ever moves through the developer's personal coach tree. This is why 33 → 34 is a hard dependency: the portfolio must exist before the transition has memberships/indexes to transfer, and before PILOT-05's zero-orphan deletion manifest has anything real to assert against.
- Roadmap (v3.0): **VAL-04 placed in Phase 34, not 33** (owner left the choice open; justification recorded). The observations it captures are pilot correction/rejection observations, produced only by the post-claim pilot contract (PILOT-01..06) and the owner's closing walkthrough (VAL-05) — both Phase 34. Its "private evaluation store, never canonical ledger, never GA4" clause IS the telemetry clause of PILOT-04's post-claim contract, so specifying them apart would invite two divergent answers to "what do we record about a pilot participant". Phase 33's coach loop emits nothing by construction (PROOF-02), so it offers no capture surface that could satisfy VAL-04.
- Roadmap (v3.0): **Phase 33 carries only two requirements by design.** Coarse granularity would normally fold a two-requirement phase into a neighbor; it stays standalone on owner direction and because its delivery surface is large (three-client imperfect portfolio + full intake→review→feedback→homework→delivery→follow-up loop on a tenant that may not mint a public token), and because collapsing it into 34 would entangle the coaching demonstration with the claim transitions that must transfer it.
- Roadmap (v3.0): the remaining ordering constraints — (a) isolation before data: RTEN-03/04's fail-closed and zero-emission properties proven BEFORE thousands of third-party records land, because retrofitting exclusion after ingestion is exactly the contamination failure mode; (b) ING-03's lossless source layer precedes evidence computation, so unknown-character/unknown-stage cases survive into EVID-02's coverage and cohort fields instead of being dropped by the legacy-match projection; (c) EVID-01 character-pair matchup evidence precedes EVID-04/05 stage evidence (a stage recommendation is undefined without the pair); (d) RPT-08's rubric + adversarial fixtures are a PHASE BOUNDARY (31→32), not an in-phase ordering, so the checkpoint-3 gate cannot be skipped; (e) FIXT-02 sparse fixtures live in Phase 31 with the engine, co-developed, because cold-start abstention exercised only after the fact is hidden by the famous-player datasets.
- Roadmap (v3.0): OWNER SCOPING SUPERSEDES RESEARCH. `research/SUMMARY.md` recommended deferring bracket prep, coaching proof, and the OAuth claim path to v3.x. REQUIREMENTS.md "Scoping authority" overrides that — EVID-07..09 → Phase 32, PROOF-01/02 → Phase 33, PILOT-01..06 → Phase 34. All 52 requirements are mapped; nothing committed was silently deferred.
- Roadmap (v3.0): security/correctness invariants are expressed as testable success criteria, never prose — zero canonical-ledger AND zero GA4 rows from research-tenant activity enforced at call sites (RTEN-04, v2.1 seeder precedent); structural absence of research tenants from public/bearer-token share/OG/recap/export/delivery routes (RTEN-03, fail-closed, not response filtering); ZERO unsupported factual claims across the adversarial acceptance suite as a stop-ship (VAL-03); identity-bound claim behavior where a historical alternate start.gg ID cannot claim and a mismatch changes nothing (PILOT-02), with the v2.4 no-merge contract extended unchanged (PILOT-03); zero-orphan deletion manifest (PILOT-05).
- Roadmap (v3.0): PROOF-03 (current-coach-tooling research + task-based pilot script) is mapped to Phase 29, not the coaching-proof phase — it is a Checkpoint-1 assumption-register artifact that must exist before the izaw workspace is designed, per GOV-01. WKSP-02 (identical engine + workflow for ordinary player-owned accounts) is mapped to Phase 32, where the workflow surfaces exist to prove parity on.
- House constraints carried into every v3.0 phase brief: RTDB conditional-spread + `.nullish()` on every stored field (never `.optional()`/`.nullable()` — 260725-juj outage class); CR-01 transaction discipline on every credit op, backfill cursor advance, and ownership flip, with FakeDatabase parity; canonical events via EVENT_CATALOG with an explicit GA4 allowlist decision per event AND research tenants emitting NOTHING; i18n ×6 for every UI string; production-gap checklist per phase brief; report jobs direct to Cloud Run via `VITE_API_DIRECT_URL`; the v2.5 activation gate and exact 0/1/2/3-failure refund math stay BYTE-INTACT (locked tests exist — a v3.0 change that breaks one is a defect, not a migration).
- Roadmap (v2.5): 14 requirements compressed into 3 coarse phases — Free Tournament Prep Brief (26, the event-bound zero-model-call surface both later phases attach to) → Contextual Paid Prep Reports Behind the Activation Gate (27, 1-credit report + bounded 3-credit/3-opponent bundle + refunds + the server-side gate) → Post-Event Review & Cited Practice Plan (28, free annotation-driven review + gated 1-credit synthesis). Strict numeric order; 27 has no placement host without 26, and 28 needs 26's date semantics plus 27's gate/refund path.
- Roadmap (v2.5): RPT-04's gate is a HARD success criterion expressed structurally — with the gate off, paid surfaces render NOTHING (no button, no disabled control, no price copy, no upsell text, no placement DOM node) and the paid endpoints answer unavailable server-side (config-null style, `CLAIM_CODE_HMAC_SECRET` precedent). No client flag can enable it. Phase 28's synthesis inherits the identical gate — no second gate, no second refund path.
- Roadmap (v2.5): EVT-04 mapped to Phase 26 (catalog extension + both emitters defined there); `prep_offer_viewed`'s firing site is Phase 27's placement — the define-here/invoke-there pattern v2.4 used for `coach_delegation_revoked`. EVT-05 → Phase 27 (existing revenue lifecycle family only, prep context as a reason code, no parallel accounting tree). EVT-06 → Phase 28.
- Phase 26 RESOLVED all three roadmap open issues (owner-locked in 26-CONTEXT.md, shipped 2026-07-29): (1) NEW event names `prep_brief_activated` (D, once per uid+entryKey) + `prep_brief_reopened` (D, after lastOpenedAt commit, client openId dedup) — Phase 13's `tournament_prep_activated` family untouched, gated by `phase13Integrity.test.ts`; (2) event date contract: "upcoming" = `firstSetAt > now`, IMMUTABLE `prepBrief.eventDate` snapshot copied at first activation — the snapshot (not the registry row) is what Phase 28's REV-01 trigger reads; (3) `prep_offer_viewed` is X-class, allowlisted in Phase 26, fired only by Phase 27's gated placement (gate-off renders nothing AND emits nothing). All three OUT of GA4 during soak with null-projection tests.
- Phase 26 contracts Phase 27/28 build on: `prepBriefs/{uid}/{entryKey}` (eventDate, activatedAt, lastOpenedAt, checklist + likelyOpponents presence maps, `.nullish()` reads normalize to {}); route `/tournaments/:entryKey/prep` (SAME URL hosts Phase 28's review mode); uid-only prep routes (no resolveSubject), GET never writes; `apps/api/src/prep/` module has an import-graph gate (no reports/billing/Anthropic deps) — Phase 27 must wire paid placements from OUTSIDE that boundary or amend the gate deliberately; `prepStructuralIntegrity.test.ts` greps ALL prep surfaces + prep i18n bundle for monetization vocabulary — Phase 27's gated UI must extend/adjust that gate deliberately (gate-on strings live outside the always-rendered surface).
- House constraints carried into every v2.5 phase brief: RTDB conditional-spread + `.nullish()` on stored fields (never `.optional()`/`.nullable()` — 260725-juj outage class); canonical events via the D-class emitter + EVENT_CATALOG with the GA4 allowlist decision explicit; CR-01 transaction discipline on every credit consumption/refund; i18n ×6 for every UI string; production-gap checklist per phase brief; report/synthesis jobs go direct to Cloud Run via `VITE_API_DIRECT_URL` (hosting rewrite timeout bypass, established v7 pattern); the activation gate is server-side config (config-null style), never client-only.
- Roadmap (v2.4): 18 requirements compressed into 3 coarse phases — Claim Credential & Atomic Ownership Transition (23, backend security core) → Coach Issuance & Client Claim Experience (24, UX + post-claim control) → Docs Housekeeping (25). Focused claim/delegation slice, not an epic; monetization (Coach Pro, capacity, queues, notifications) stays gated for v2.5.
- Roadmap (v2.4): Phase 23 lands the ENTIRE server-side security surface first — leak-proof CSPRNG codes (digest-at-rest), no-oracle errors, per-account+network rate limits, the CR-01 atomic idempotent transition (same-tenant control flip, coach→delegated), delegation-gated access with immediate stale-access blocking, the no-merge collision contract, and the full canonical event catalog. Phase 24 (UX) cannot start until 23 lands — hard sequencing constraint. Docs (25) sequenced last to capture as-shipped behavior.
- Roadmap (v2.4): Security-critical properties are success criteria, not prose — no-oracle (identical error body + timing class for expired/used/revoked/mismatched/conflicting/never-issued), digest-at-rest (raw code grep-clean across logs/analytics/URLs/audit), immediate stale-access blocking (403 next request on revoke/never-granted), and no-merge (transaction refuses to merge/overwrite/dedupe/move/pick-a-history).
- Roadmap (v2.4): CLAIM-04 (no-merge) mapped to Phase 23 (backend enforcement is the guarantee); Phase 24 renders the collision choice as the collision variant of CLAIM-01's confirmation. EVT-03's `coach_delegation_revoked` emitter defined in Phase 23's single EVENT_CATALOG extension, invoked from Phase 24's CTRL-02 revoke action. UI hint on Phase 24 only (Phase 23 is backend-only; Phase 25 is docs).
- Roadmap (v2.4): REQUIREMENTS.md self-count said "17 total" but the actual REQ-ID count is 18 (CRED×5 + CLAIM×4 + CTRL×3 + ENTRY×2 + EVT×3 + DOCS×1) — corrected to 18/18 mapped, no orphans. Same off-by-one class as v2.2's "20 vs 21".
- House constraints carried into every v2.4 phase brief: RTDB conditional-spread + `.nullish()` write pattern (never `.optional()`); all client-workspace routes ride the foreign-client 403 test harness; i18n ×6 for every new UI string (Phase 24); CR-01-style transactions for the atomic claim; production-gap checklist per phase brief; canonical events via the established D-class emitter + EVENT_CATALOG.
- Carried from v2.0/v2.1: redaction-by-shape; soft-revoke + hard-delete tokens; RTDB CR-01 null-first-run discipline; GA4 demoted to best-effort projection (first-party RTDB ledger is source of truth); no video hosting (embed/deep-link only); actor identity ≠ performance subject; X-Active-Subject derives from clientId presence only.
- Phase 23 planning (2026-07-24, owner decision): destructive coach routes (`deleteClient`/`archiveClient`/`exportClient`) become role-gated to `['custodian','owner']` in Phase 23 — a demoted `'delegate'` coach must not be able to destroy/archive/export the workspace the client now owns. Read/review/session/delivery routes stay open to delegates (that is the coaching relationship). This OVERRIDES RESEARCH.md Assumption A5, which proposed leaving them role-blind until Phase 24. Rationale: `requireMembership` is existence-only, so without this, success criterion 4 ("coach demoted to delegated collaborator") would be aspirational rather than enforced.
- Phase 23 planning (2026-07-24, owner decision): no-oracle response floor fixed at **200ms** (`CLAIM_REDEMPTION_FLOOR_MS`). Primary CI coverage is the STRUCTURAL property (identical RTDB call shape/count across all five failure classes), not a wall-clock assertion — exactly one timing test, and it only proves the floor fires.
- Phase 23 planning (2026-07-24): rate-limit counter retention/pruning DEFERRED — `jobs/prune.ts` not extended for `claimRedemptionAttempts*`/`claimIssuanceAttempts`. Low write volume at current scale; recorded as a decision, not an oversight. Revisit if counter trees grow.

### Deferred Verification

| Phase | State | Resume |
|-------|-------|--------|
| 30 | correction_required_before_human_uat | Do NOT run the current live backfills and do NOT advance to Phase 31. First run `/gsd-phase --insert 30 Demo Account Topology & Migration`, reconcile requirements/roadmap/UAT, migrate or reprovision without data loss, then resume the corrected owner UAT. |

Phase 30 detail (2026-08-08): 8/8 plans executed and merged (30-08 stopped 3/5 tasks at its DESIGNED blocking owner-evidence checkpoint — Tasks 4-5 are the owner gate + closure). VERIFICATION status `human_needed`, 44/44 automated must-haves verified; suites shared 718 / api 2528+1 / web 2238 green at `86c633fa`; v2.5 money/gate + Phase 29 locked contracts byte-unchanged. `30-COVERAGE-AUDIT.md` carries 15 OWNER EVIDENCE PENDING markers — VAL-01/WKSP-01A stay open until they're replaced with observed evidence (never close a claim while a marker remains).

(Phase 24's earlier deferred verification RESOLVED 2026-07-28 — owner validated, status passed.)

### Pending Todos

- **[OWNER, critical path] Codex hard-gate review of `6561a471`**, then Gate 6 execution per the committed runbook. Production is NOT running any Gate 6 code (probed: `/api/deployment-identity` → 404); the P1 deploy is the first real step and everything past it depends on it.
- **[OWNER, independent] Phase 30.1 step-10 archive sign-off** — archive after sign-off or deliberately retain the frozen baseline; see `phases/30.1-demo-account-topology-migration/30.1-ARCHIVE-DECISION.md`. Never delete the source tenants. ACCT-02 stays pending until decided.
- **[HYGIENE] 14 `worktree-agent-*` worktrees** remain under `.claude/worktrees/`; all their work is merged. Safe to remove + `git worktree prune`. Do not resume them expecting fresh context.

- **30.2 Gate 2 apply (OWNER, critical path — corrective directive).** ALL prior manifests (`55ea0d…`, `0bcaf1de…`, every `enrichment-manifest*.json`) are SUPERSEDED and must NOT be applied — Gate 1 changes parser/validation versions and apply rejects older-version manifests. Only path: after Gate 1 lands + CI green + Codex pre-apply gate, generate a FRESH schema-parsed manifest, compare to reference expectations (MkLeo 57 / Sparg0 68 / hbox+izaw 0, deltas explained by source/parser revisions only), then serialized per-account apply (hbox verified as completed no-op; MkLeo → audit; Sparg0 → audit; IzAw honest zero). See 30.2-CORRECTIVE-DIRECTIVE.md Gate 2.
- **30.1 step 10 soft-archive sign-off (OWNER, independent).** Steps 1-9 done (manifest `ce09412e…d6dbb`). Never delete source tenants.
- ~~CI run 31901367187 on `ac6f5cf6` in_progress~~ **RESOLVED 2026-08-15 (audit F-03)**: confirmed `completed`/`success` via gh (warm-cdn subdirectory fix run); master through `ac6f5cf6` is CI-green.
- **Deferred (Codex-advisor scoping): /api/matches payload follow-up** — verify br/gzip end-to-end through Hosting in a signed-in HAR; scope pagination/field-trimming for 8k+ match accounts as its own task.
- **Stray untracked file `f057e55a3b53a45fa58d48e106dabcd234af2e64`** at repo root (plus `.VSCodeCounter/`, `discussion-context/`) — inspect/remove when convenient; other untracked files may belong to concurrent sessions.

- ~~Pending API deploy for quick 260726-r7~~ **RESOLVED 2026-07-28**: the "not deployed" claim was a timezone arithmetic error (19:06:24Z read as 12:06 local via UTC-7; machine is UTC-4, so revision `00069-fpj` went ready 15:06 EDT — 19 min AFTER r7's 14:47 merge, same deploy session as the 15:07 web dist). Owner's re-check confirmed r7 behavior live (workspace delete stays gone, orphans self-heal).
- ~~v2.4 archival trap~~ **HANDLED 2026-07-28**: v2.4 lifecycle driven from the preserved copies via temporary swap (v2.5 files backed up to session scratchpad, restored after archival). Phase 24 verification flipped honestly after owner validation — not hand-forced.
- ~~DEPLOY NEEDED~~ **DEPLOYED 2026-07-31 (owner-authorized, run by Fable)**: Cloud Run revision `smash-tracker-api-00072-j5v` (ready 16:30Z) + hosting (`index-BZSULi4N.js` verified served). Carries Phases 26+27, the P0 event fix, clear-vod fix, binding fix, and quick 260731-a1/b1. Verified post-deploy: prep routes answer 401/400 (no longer 404), `PREP_PAID_REPORTS_ENABLED` confirmed ABSENT from revision env (gate OFF per RPT-04), all prior secrets carried over. **Soak clock started 2026-07-31 → funnel-readout review ≈ 2026-08-14.** First check when back: re-run the eventLedger probe to confirm D/B events are landing again organically.
- **Prod residue from Phase 27 UAT round 1** (acknowledged, harmless): "Phase 27 UAT Sep 1 2026" event + prep brief + 5 curated opponents + 3 no-charge bindings on prod RTDB; no deletion endpoint exists by design. Orphaned eventDedup markers (claim_completed ×2, coach_delegation_granted ×2, prep_brief_activated ×1, …) left in place — deleting could re-fire one-shot events.
- **Lead, untested**: `useSubjectPath.ts` may build personal-scoped paths on `/workspace/*` routes (surfaced during the r8 audit, out of scope there). Symptom: a link inside a claimed workspace navigating to its personal equivalent.
- **Cosmetic**: raw UID rendered in the Coach access card ("Delegated coach: i7UM...") should resolve to a display name; a client created without a name renders as its raw tenantId in the coach hub.
- **2 stale `worktree-agent-*` branches remain** (`a7d6312bc4da5dd32`, `ad81214fa95d0b5dd`), both still holding registered worktrees so they are correctly undeletable. An earlier claim of "~32 stale branches" was recalled rather than probed and was wrong — probed count on 2026-07-28 is 2. Remove the worktrees first if you want them gone.
- **Decision-coverage gate parser mismatch (Phase 23, non-blocking).** `check.decision-coverage-plan` and the plan:post gap analysis both return "extracted 0 of N — possible format mismatch" against `23-CONTEXT.md`: the discuss-phase wrote categorized prose bullets under `### Area` headings rather than the `- **D-NN:** text` form the parser requires, so zero trackable decisions were extracted (`uncovered: []` — no real gap). Coverage was verified manually instead: all 23 locked-decision markers appear in executable plan content. Either adopt `D-NN` numbering in future CONTEXT.md files or accept that this gate no-ops for this project.
- **`state.planned-phase` did not write STATE.md** during Phase 23 planning (returned `{"updated": []}`); the Current Position / plan counts above were corrected by hand. Worth checking before relying on that verb again.

### Quick Tasks Completed

| # | Description | Date | Commit | Directory |
|---|-------------|------|--------|-----------|
| 260812-qit | **P1 login boot stall fixed** (ultracode; owner-declared priority 1): HAR proved five statically-imported entry chunks hung 26-103s on a post-deploy cold CDN edge and boot hard-blocked — NOT the API (3.67MB /api/matches took 2.5s). Cut the entry graph 62→27 modulepreloads (lazy MainLayout+SignInCard with warm-up imports, en locale inlined via base-code loader map), added retryableLazy (timeout/retry/cooldown-reload) on all 40 routes, ChunkErrorBoundary (6 locales), inline boot spinner + 15s watchdog, hardened prerender gate on the sign-in card, `pnpm warm:cdn` post-deploy smoke/warm script — now wired into `firebase.json` `hosting.postdeploy` so it runs on every `firebase deploy --only hosting`, covering the CI-wait→merge→deploy watcher scripts that a package.json chain would miss (verified from repo root: 398 paths × 2 encodings, 26s; exits 1 on content-type mismatch, which correctly flagged local dist being ahead of prod) — and @fastify/compress (br/gzip) on the API. Adversarial review workflow: 19 confirmed findings all addressed (incl. critical pt-BR→English i18n regression and an infinite-reload-loop guard defect). Web 2316/api 3103/typechecks/lint/prerender green. NOT yet deployed — ships with the step-6 deploy of the 30.1 owner order | 2026-08-12 | 9e3f812d,72e09750,b1c72fc9,b113f15f | [260812-qit-cold-cdn-boot-stall](./quick/260812-qit-cold-cdn-boot-stall/) |
| 260810-cif | (gsd-fast) De-flaked CI: MatchDataPage VOD-edit interaction test timeout 10s→30s (#159 flake class; failed on a runner with 215s environment setup, passes ~2s locally; test file untouched since July so no regression). Test-only | 2026-08-10 | 2a0c69f | — |
| 260810-fbp | (gsd-fast) Read-only `freezeBaselineProbe.ts` for Phase 30.1 Task 2.6: per-tree counts for all 7 copy trees via the migration module's own `enumerateRecords` (byte-comparable to the later copy manifest), activeRunId + coverage asOf per tenant, assert-empty existence checks; exits non-zero on any active run/missing coverage/violation. Owner-run, zero writes; typecheck+lint+prettier clean | 2026-08-10 | 1b58327 | — |
| 260810-iw5 | Fixed post-signup black screen (profile 404 race, found by owner creating demo account #1): `provisionUser` now returns a success boolean and new `provisionAndRefreshProfile` invalidates `['profile']` only on success, awaiting the FB-01 uid-transition cache-clear chain first (all 5 provisioning call sites); HomePage's signed-in-no-profile branch renders existing `chrome.loading` instead of null (signed-out/prerender branches byte-unchanged, zero new i18n). Deterministic deferred-mock race regression test; web 2253 green, typecheck + lint clean. NOT yet deployed — ships with the step-6 deploy of the 30.1 owner order | 2026-08-10 | e6b5e5a,90c88a5 | [260810-iw5-fix-post-signup-black-screen-invalidate-](./quick/260810-iw5-fix-post-signup-black-screen-invalidate-/) |
| 260806-k1z | Turnkey local development: root `pnpm dev` loads the API workspace env; Vite development pins all report submissions to the local API and the CSP permits exactly `http://localhost:3001`; README documents production-backed test-account risk, emulator isolation, and secret hygiene. Focused API/web tests, both typechecks, and formatting green; no env, build, deploy, or lockfile artifacts touched | 2026-08-06 | c38c923 | [260806-k1z-make-local-development-turnkey-pnpm-dev-](./quick/260806-k1z-make-local-development-turnkey-pnpm-dev-/) |
| 260806-i89 | Remediated all 14 open Dependabot alerts via 2 atomic commits: fast-uri 3.1.3→3.1.5 (CVE-2026-18446/16221, genuinely runtime-reachable on the Fastify ajv/fast-json-stringify request path), find-my-way 9.6.0→9.7.0 (CVE-2026-47219 HTTP/2 DoS), protobufjs 7.6.4→7.6.5 + fast-xml-parser 5.9.3→5.10.1 (via firebase-admin), undici/postcss/brace-expansion bumped for hygiene only (dev/build-time, GitHub's `scope=runtime` label doesn't hold for these 3), react-router 8.1.0→8.3.0 (RSC-CSRF advisory doesn't apply to this hash-routed SPA). Diagnosed PR #169's CI failure as a stale-base artifact (de-flake commit 6da6a6d absent from that PR's base) unrelated to react-router — confirmed locally, full web suite green incl. cap test. All gates green (shared 574/api 1628/web 2079); nothing pushed, PRs #169/#170/#171 untouched pending push-timing decision | 2026-08-06 | bf2f708,06b2833 | [260806-i89-validate-dependabot-alerts-fast-uri-cve-](./quick/260806-i89-validate-dependabot-alerts-fast-uri-cve-/) |
| 260806-hzx | Fixed stale-report-job sweep dropping `reason` on terminal write (location correction: bug is in `jobs/sweepStuckReportJobs.ts`, not `routes/reports.ts` as described — that route's `failJob()` already conditional-spread `reason` correctly). Sweep now carries `reason`/`resultRef` through the terminal `set()` and `report_failed` event, same conditional-spread pattern as `failJob()`. Without the fix, a swept `prep_bundle` child looked reason-less to `reports.ts`'s `preSpent` gate, letting a replayed retry spend a fourth credit and generate a free report on an already-refunded slot. Terminal status deliberately stays `failed` (Phase 28 CR-02). 5 new tests (4 sweep unit + 1 route integration, verified as a genuine pre-fix-failing regression); API gate green: 1633 tests, typecheck clean, lint 0 errors | 2026-08-06 | 9d01a25,27ea5dd | [260806-hzx-fix-stale-job-sweep-reason-loss-in-repor](./quick/260806-hzx-fix-stale-job-sweep-reason-loss-in-repor/) |
| 260806-hzx | Phase 28 review follow-up (CR-01 item 4 parenthetical): prep_report/prep_bundle arms of POST /api/reports accepted client jobId/bundleId with RTDB-illegal chars (. # $ [ ] / space, ctrl) → 500 on reportJobs/{uid}/{jobId} write. Swapped both fields on generateReportRequestSchema to entryKeyInputSchema (WR-06 4b5623f template; field-level so legacy arm covered too); 7-char schema batteries + route-level 400 batteries + positive bundle-child `{bundleId}:{slot}` retry-contract lock. shared 577 + api 1630 tests green, typechecks clean | 2026-08-06 | d156c31 | [260806-hzx-validate-client-supplied-jobid-and-bundl](./quick/260806-hzx-validate-client-supplied-jobid-and-bundl/) |
| 260731-b1 | Review composer + delivery UX: evidence notes expand/collapse in place; inserted {{cite}} label truncated to 40 chars (~660→~110 char tokens, stored grammar/rendering byte-identical); session-delivery homework now client-interactive on /r/:token (ack card, per-item checkboxes, non-locking submit) with server-persisted progress via two anonymous rate-limited no-oracle routes; coach menu shows completion count + stamps. +14 i18n keys ×6 | 2026-07-31 | dea0747,3b5c164,4c2eaa0 | [260731-b1-review-citations-homework-interactive](./quick/260731-b1-review-citations-homework-interactive/) |
| 260807-ctw | Stage picker: Standard section now holds the 11 tournament-legal stages (was online trio); shared TOURNAMENT_LEGAL_STAGE_IDS + membership/order/precedence tests; GSP quick logger keeps trio deliberately | 2026-08-07 | 30b1d56,97a32f0,2f30418 | [260807-ctw-stage-picker-add-standard-legal-stage-se](./quick/260807-ctw-stage-picker-add-standard-legal-stage-se/) |
| 260807-cty | Client delivery Review Notes readability: sections now the flexible main column (coach parity), citation player demoted to sticky rail, inner scroller removed, no-citations case full-width with noSource as footnote | 2026-08-07 | 6052ed7,c86c715 | [260807-cty-client-delivery-review-notes-readability](./quick/260807-cty-client-delivery-review-notes-readability/) |
| 260807-jyt | VodManagerPage detail panel: details card was auto-flowed into an implicit grid row far below the player whenever the notes rail outgrew it (all three VOD routes) — primary column collapsed to one flex wrapper, rail single-row, details snug under the reserved player box in every load/error state | 2026-08-07 | f8fe58a,a3264b9 | [260807-jyt-client-workspace-vods-video-details-card](./quick/260807-jyt-client-workspace-vods-video-details-card/) |
| 260731-a1 | Set entry: per-game Your/Opponent fighter pickers in Bo3/Bo5 wizard (game 1 ← set-level, game N ← game N−1, per axis; buildSetGamePayloads signature unchanged so all 3 callers untouched). VOD manager: set-scoped lists (synced setId or ≤5 manual games sharing opponent/event within 60s) default oldest-first — game 1 on top; explicit sort choice still wins; sort-control a11y label fix. +5 i18n keys ×6 | 2026-07-31 | d2eef19,b6ecf0e,b95b34c | [260731-a1-set-entry-per-game-characters-vod-set-order](./quick/260731-a1-set-entry-per-game-characters-vod-set-order/) |
| 260726-r8 | Mutations invalidated the PERSONAL cache namespace inside a claimed workspace (writes landed server-side; UI stale until refresh). Converted 8 mutation hooks to useEffectiveSubject: useCreateMatch/useUpdateMatch/useDeleteMatch/useSaveFighters + all 4 useVodNotes. Tests assert the exact invalidated key across workspace/personal/coach routes | 2026-07-28 | fda5eb7,7dfc888,3cc75b4,b4678df,523e61e | [260726-r8-mutation-cache-scope](./quick/260726-r8-mutation-cache-scope/) |
| 260726-r7 | **P0 data integrity**: deleting a claimed workspace orphaned its clientOwnedTenants reverse-index row (Phase 24 added the index; Phase 23 cascade never learned it) — client saw undeletable dead workspaces. deleteClient now resolves the owner uid from clientMembers and nulls the index in the same atomic update; listOwnedWorkspaces excludes + best-effort prunes dead rows (self-heals existing orphans on next read). Full audit: clientOwnedTenants was the sole gap | 2026-07-28 | ead8159,8a08118 | [260726-r7-orphan-workspace-cleanup](./quick/260726-r7-orphan-workspace-cleanup/) |
| 260726-r6 | Owner can hard-delete their claimed workspace: Danger zone card on /workspace/:tenantId/overview (never on the coach client page — negative-render tested), DeleteWorkspaceDialog requiring the exact case-sensitive workspace label, wired to the existing DELETE /api/coaching/clients/:clientId (no server change; requireTenantRole custodian|owner already denies demoted delegates). +10 i18n keys ×6 | 2026-07-26 | aaba4f9,38feb1d,273b209,ad940e4 | [260726-r6-owner-delete-workspace](./quick/260726-r6-owner-delete-workspace/) |
| 260726-r5 | Phase 24 gap closure: OwnerWorkspaceEntryChip in Topbar (desktop+mobile) gives claimed clients a discoverable route into their workspace from anywhere outside it (0 owned → nothing, 1 → direct button, 2+ → dropdown); useCoachAccessEjection boots a revoked coach to /coach on a 403 scoped to the active client cache namespace. Coach chrome additive-only, guarded by a new coachChromeIntegrity grep gate. +5 i18n keys ×6 | 2026-07-26 | 84f9c5c,024a55b | [260726-r5-workspace-nav-and-revoke-boot](./quick/260726-r5-workspace-nav-and-revoke-boot/) |
| 260726-r4 | **P0 data loss (pre-existing since v2.0 Phase 11-08)**: saving one fighter slot wiped the other — read-modify-write with `?? []` (unknown→empty) against replace-semantics PUT. Also fixed sibling-refetch discarding unsaved edits, and stopped combined pages navigating away mid-setup. New useFighterSlotSelection hook; coach + owner fighter pages now send one coherent {primary,secondary} | 2026-07-26 | c869b5e,11b54aa,c668205 | [260726-r4-fighter-save-data-loss](./quick/260726-r4-fighter-save-data-loss/) |
| 260726-r3 | Add missing stage Hollow Bastion (id 118): StageList entry + official-source art (script-extended acquire-stage-art.mjs, visually verified) + stageArt count 117→118 + fixed latent start.gg 513 mapping no-op | 2026-07-26 | a64a75f,6147d0d,3e9f56e | [260726-r3-hollow-bastion-missing-stage](./quick/260726-r3-hollow-bastion-missing-stage/) |
| 260726-r2 | Share/delivery note-snapshot caps 20→MAX_VOD_TIMESTAMPS_PER_MATCH (includedVod/shareSnapshot/publicShareSnapshot timestamps + deliveryVodFreeze slice); sets/citationSources/homework caps deliberately left with inline docs | 2026-07-26 | 9ef1c92,d7b3480,9244bc6 | [260726-r2-share-snapshot-caps-1000](./quick/260726-r2-share-snapshot-caps-1000/) |
| 260726-r1 | Review delivery page fluid desktop width: /r containers max-w-4xl→6xl/1440/1600 by breakpoint, notes rail capped ~420px at xl (player absorbs width); /s widened on shallower curve | 2026-07-26 | 780a17c,caa3ef2 | [260726-r1-review-page-fluid-width](./quick/260726-r1-review-page-fluid-width/) |
| 260725-q4 | Desktop two-column delivery layout: /r/:token player left + notes right at lg:, notes column scrolls internally (max-w-4xl container; mobile unchanged) | 2026-07-25 | 30a25ef,b05f0f3 | [260725-q4-delivery-two-column](./quick/260725-q4-delivery-two-column/) |
| 260725-q3 | VOD timestamp cap 20→1000 via new shared MAX_VOD_TIMESTAMPS_PER_MATCH; fixed VodNotesDialog duplicate-constant drift; share/delivery snapshot caps deliberately unchanged | 2026-07-25 | 14a77c6,d4bb27a,ae7d2bb | [260725-q3-timestamp-cap-1000](./quick/260725-q3-timestamp-cap-1000/) |
| 260725-q2 | Timestamped-note EDIT mode is now a multi-line auto-grow textarea (VodManager + /s coach edit); Ctrl/Cmd+Enter saves with visible hint x6 locales | 2026-07-25 | a35d6ab,8bfa1e0 | [260725-q2-note-edit-textarea](./quick/260725-q2-note-edit-textarea/) |
| 260725-q1 | Fighter pickers sort alphabetically by LOCALIZED name (locale-aware localeCompare) across all selection surfaces; roster-order sources replaced by useSortedFighters/useAlphaFighters | 2026-07-25 | f401693,fe54cd8 | [260725-q1-fighter-picker-alphabetical](./quick/260725-q1-fighter-picker-alphabetical/) |
| 260725-juj | INCIDENT FIX — archiving a never-published review wrote `latestVersion: null`, RTDB stripped it, `.nullable()` read schema then threw → `listClients`/reviews 500 → coaching looked wiped (data was 100% intact, verified live). Fix: `.nullish()` stored-shape read schema + normalize, conditional-spread write (both reviewStatus write sites), per-tenant degrade in listClients, Client Hub error/loading states (loadError keys ×6 locales), unknown≠zero guards in GuidedPathCard/DashboardPage. Live-verified against prod RTDB post-merge (6/6 clients). API-side — needs Cloud Run deploy to cover prod | 2026-07-25 | 5f3150c | [260725-juj-fix-coaching-outage-null-stripped-review](./quick/260725-juj-fix-coaching-outage-null-stripped-review/) |
| 260724-iu1 | Client delivery/share view + VodNotesDialog: wrap long timestamp notes to multi-line (3 sites, same treatment as 260724-hwa; 55 tests green) | 2026-07-24 | f3f9a24 | [260724-iu1-client-delivery-share-view-long-timestam](./quick/260724-iu1-client-delivery-share-view-long-timestam/) |
| 260724-icb | Fix IN-06: /s share shell blanked ≤5 min after every hosting deploy — getShell now ETag-revalidates spa.html on every request (304 = cheap reuse, fresh 200 picked up immediately, stale-on-error fallback when cached; 15 tests incl. 5 new). API-side — live on next Cloud Run deploy | 2026-07-24 | b23977a | [260724-icb-fix-in-06-s-share-shell-goes-blank-for-5](./quick/260724-icb-fix-in-06-s-share-shell-goes-blank-for-5/) |
| 260724-hwa | VOD manager: wrap long timestamp notes to multi-line (was truncate + rail horizontal scrollbar; min-w-0 + whitespace-normal break-words, items-start) | 2026-07-24 | 56c1d07 | [260724-hwa-vod-manager-timestamped-notes-wrap-to-mu](./quick/260724-hwa-vod-manager-timestamped-notes-wrap-to-mu/) |
| 260724-ggh | Fighter-setup UX (VOD-manager empty-fighters callout w/ context-aware Set-fighters CTA, self-explaining disabled Add Match trigger, client-create toast nudge) + addVod label rename ×6 + double-plus label fix ×6 + EditMatchForm stage-form round-trip test (4 commits; lint 0 err, tsc clean, 1627 web tests green) | 2026-07-24 | 718f9bd | [260724-ggh-fighter-setup-ux-for-clients-vod-manager](./quick/260724-ggh-fighter-setup-ux-for-clients-vod-manager/) |
| 260723-ws0 | Fix web lint error failing master CI: react-hooks set-state-in-effect in PendingButton spinner effect (reset moved to effect cleanup; UXFB-01 semantics preserved; lint 0 errors, tsc clean, 1616 web tests green) | 2026-07-24 | 3004f75 | [260723-ws0-fix-web-lint-error-failing-master-ci-rea](./quick/260723-ws0-fix-web-lint-error-failing-master-ci-rea/) |

*(Older quick tasks: see prior STATE history / .planning/quick/.)*

### Blockers/Concerns

- ✅ **[GOV-02 RESOLVED 2026-08-08 — Phase 30 unblocked]** Owner correction `GOV-02-STARTGG-SCOPE-R1` (issued in-session, same human channel as the original [HUMAN] gate, superseding the same-day Q3 "Block everything pending" answer): written start.gg clarification is NOT required for the private ingestion/analysis/visualization/development/demonstration scope. Q1 scope conditions still govern (official API + published rate limits, provenance, explicit missingness, retention/deletion support, research-tenant isolation, no redistribution/endorsement/fabricated identities/automated contact/public surfaces/charging). Re-escalate only on API rejection, a directly applicable prohibition, or materially broader use. Full record: `29-STARTGG-POLICY.md` §Supersession record.

- 🔴 **[P0 INCIDENT 2026-07-30 — soak VOID, restart at fix deploy]** Every D/B-class canonical event write in prod FAILED from 2026-07-19 (Phase 10 deploy) to the pending fix deploy: envelope builders emitted `artifactKind: undefined` own-properties (Zod .optional() preserves them; JSON hides them), the real RTDB SDK rejects them AFTER createEvent's dedup marker committed → events permanently swallowed (orphaned markers, no ledger rows; only X-class events landed). Found by the owner's Phase 27 walkthrough; confirmed by read-only prod probe. Fixed in `49e8f5e` (+ FakeDatabase now rejects undefined like the real SDK — the class fails in tests now; + createEvent is total, so telemetry failures can't kill the process between a credit debit and its refund). **The ~Aug-2 soak window is INVALID — the 2-week RPT-04 soak clock restarts when the fix deploys** (≈ deploy + 14 days before `GET /internal/jobs/funnel-readout` means anything). Ledger data July 19–30 is unrecoverable; orphaned eventDedup markers left in place deliberately.
- ⚠️ [v2.5 Phase 27/28] Money-safety phases — credit spend/refund correctness is the hard invariant. Any double-charge, missing refund on a failed job, non-idempotent refund under replay, unbounded bundle selection, or paid affordance leaking through the gate is a stop-path. Plan for CR-01 transaction discipline (the null-first-run local-cache hazard) and FakeDatabase parity from the start; extend the existing `POST /api/reports` job machine rather than forking a second pipeline.
- ⚠️ Stale .planning/codebase maps (mapped 2026-07-09) — v2.5 phases touch tournaments/reports/billing, which HAVE drifted; prefer live code reads over the maps during planning.

### Roadmap Evolution

- Phase 30.2 inserted after Phase 30.1: Liquipedia Stage & VOD Enrichment (ENR-01..13) — owner direction 2026-08-11; hard gate before Phase 31 (URGENT)

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Stage 4 later workflow (v2.5+) | Multiple/archived clients, claimed-client queue/inbox, privacy-safe notifications, capacity enforcement, customer portal, Coach Pro trial (S4L-01..03) | gated on v2.4 claim slice proving safe | 2026-07-24 |
| Stage 4 later workflow (v2.5+) | Guided merge path for claim collisions (S4L-04); `claimed_workspace_exported` portability (S4L-05) | separately approved flow / later | 2026-07-24 |
| Share refinement | Token rotation, per-note redaction, player/season card variants (SHRX-01..03) | v2+ | 2026-07-15 |
| Growth | Referral credits, AI annotation-synthesis, public/indexed shares (GRWX-01..03) | v2+ | 2026-07-15 |
| Strategy (evidence-gated) | Player cards (Stage 5), tournament lifecycle (Stage 6), qualified referrals (Stage 7), crew discovery (Stage 8) | deferred at v2.0 def | 2026-07-18 |
| Tech debt (v1.1 audit) | IN-07/IN-08 info-grade race edges; stale .planning/codebase maps | backlog | 2026-07-18 |

## Session Continuity

Last session: 2026-08-09 (into 08-10)
Stopped at: OWNER CORRECTION absorbed → Phase 30.1 (Demo Account Topology & Migration) planned+converged (3 Codex cycles, 5 HIGH→0) and **Wave 1 EXECUTED + merged to master at `47118b6`** (30.1-01 migration engine, 30.1-02 IzAw coaching topology, 30.1-03 demo-uid isolation, 30.1-05 self-account coverage panel — post-merge gate green: api 2623, web 2243, lint clean, locked contracts byte-unchanged). **Wave 2 = Plan 30.1-04 is OWNER-GATED and paused, and its execution order was CORRECTED by the owner 2026-08-10** into a 10-step sequence (see Operator Next Steps): complete + freeze the partial sources BEFORE copying, and push/CI/deploy BEFORE the copy because the in-account coverage UI and demo isolation must be live for the production login checks. Master ~65 commits unpushed (push is step 5, an owner action).
Resume file: this section + the Phase 30.1 plans in `.planning/phases/30.1-demo-account-topology-migration/`.

## Operator Next Steps

- **Phase 30.3 Gate 6 — RUNBOOK IS THE SINGLE AUTHORITATIVE, COMMITTED SOURCE, now on pass 8 (2026-08-16).** `.planning/phases/30.3-demo-usability-closeout/30.3-UAT-CHECKLIST.md` is git-tracked (a reviewed-gate exception to `.planning/`'s gitignore) and has been through seven correction passes since the original Codex hard-gate ADJUST — full history in `stopped_at` above; **do not restate its commands here or anywhere else, read the file directly.** Pass 7 (this session) fixed: P1 doing two API deployments instead of one reproducible `docker buildx` build+push+deploy; hosting deploy missing a runnable pre-build env-var assertion and a pinned `--project`; P5/P7 offering a non-mandatory "fewer than four accounts" option (deleted — the gate is unconditionally four-account); and P4–P7 being interleavable with the unbounded Section S manual walkthrough, which could expire P4's 6h registry manifests (reordered so P4–P7 complete first, manual UAT strictly after). Two items (a second capture flag, a mixed-revision guard) are LANDED at 713b5742 (release-SHA binding + mixed-revision headers; see stopped_at); the coordinator will send the final capture invocation. Next action: owner/Codex review of the committed §P (G7 → P1–P7), then execute it for real. Do NOT run any registry apply against production, and put no ID token value in any planning artifact, until that review happens. This supersedes every earlier "P1–P6"/"P1–P7" one-liner above it and in the further-superseded `stopped_at` entries below, and supersedes the P-sequence in every `.planning/HANDOVER-*.md` file — those now point back to the committed checklist rather than restate commands.
- **Phase 30.2 Gate 2 — CORRECTIVE DIRECTIVE SUPERSEDES ALL PRIOR B7 INSTRUCTIONS (2026-08-15).** Do NOT apply any existing `enrichment-manifest*.json` (`55ea0d…`, `0bcaf1de…`, or older) — two apply attempts aborted at the write boundary and Gate 1 makes apply reject manifests from older parser/validation versions. The only production-apply path is `30.2-CORRECTIVE-DIRECTIVE.md` Gate 2: Gate 1 reliability fixes shipped + CI green → Codex pre-apply gate → fresh schema-parsed manifest → reference-expectation comparison → serialized per-account apply with per-account postcondition audits. Do not archive the four Phase 30.1 source tenants as part of any of this.

- **Phase 30.1 Wave 2 — OWNER-CORRECTED 10-STEP ORDER (2026-08-10).** Plan 30.1-04 rewritten to this sequence; do NOT reorder. Two corrections drove it: the sources hold PARTIAL data (15 `OWNER EVIDENCE PENDING` markers) so they must be COMPLETED + FROZEN before any copy (ordinary destination accounts may not support advancing research-tenant backfill jobs, so a partial copy is not completable at the destination); and push/CI/deploy are NOT independent of migration validation, because the Plan-05 in-account coverage UI + Plan-03 demo isolation must be LIVE for the required production login checks.
  1. ✅ DONE 2026-08-10 — migration CLI built + fully tested, zero production contact (merged `be5f389`; api 2639 green, 17-case pure validator suite)
  2. ✅ DONE 2026-08-10 — four demo accounts created via prod sign-up UI; uids recorded (UIDS ONLY) in 30.1-04-SUMMARY.md §Task 2 Owner Gate. Hbox `B4AoA73kJ2dlk9B61POUsTUjM6w2` / MkLeo `eVJih9SgfJVk5oMPAQydPGbEBpU2` / Sparg0 `cosPe2wagVZsTWprGKnpjrcUEsb2` / IzAw `VnWOqNRRP5ZqFF0457DPqiBsT4D3` *(Task 2)*
  3. ✅ DONE 2026-08-10 — all four Phase-30 SOURCE backfills terminal and published; identities confirmed; zero active runs; 12/12 fresh official tournament spot-checks delta 0; all 15 pending markers resolved; provider-unavailable ranges disclosed *(Task 2.5)*
  4. ✅ DONE 2026-08-10 — frozen baseline recorded; zero active runs, all copied/assert-empty trees verified *(Task 2.6)*
  5. ✅ DONE 2026-08-10 — pushed through `2a0c69f`; CI green after de-flaking the known MatchDataPage timeout *(Task 2.7)*
  6. ✅ DONE 2026-08-10/11 — `DEMO_ACCOUNT_UIDS` configured; API + web deployed and verified *(Task 2.8)*
  7. ✅ DONE 2026-08-11 — all four exact uids authenticated, coverage-null endpoint healthy, mint refused 403 ×4; fifth non-listed account and unrelated `/s` + `/r` public deliveries remained healthy *(Task 2.9)*
  8. ✅ DONE 2026-08-11 — production copy completed cleanly after one idempotent replay; manifest `ok=true`, hash `ce09412e…d6dbb` *(Task 3)*
  9. ✅ DONE 2026-08-11 — all frozen counts/ids/values exact; deployed match counts 8582/4568/8378/237; populated coverage rendered for all four; IzAw Coaching mode true *(Task 3)*
  10. After EXPLICIT owner sign-off, run the SEPARATE soft `archive` — never a permanent delete *(Task 4)*
  Then: Phase 30.1 verification → `/gsd-autonomous --from 31 --converge --codex`.

- **Phase 30.1 VERIFICATION NOW EXISTS (2026-08-16) — `human_needed`, 3/6 must-haves, `30.1-VERIFICATION.md`.** The phase previously had NO verification report at all. Read-only pass, zero production contact. Verified: the migration engine (module-load-locked descriptor registry, `researchEntitlements` correctly `assert-empty` not `copy`, global assert-empty pre-pass, per-record conditional `transaction()`), CLI archive gating (hash + dbIdentity binding + strict schema + exact-four-map + live read-only re-verify, every failure path exiting before `archiveSources`), IzAw coaching topology, all 7 demo-isolation chokepoints, the self-only coverage surface, and the production copy itself — the canonical SHA-256 of `apps/api/migration-manifest.json` was RECOMPUTED locally and equals `ce09412e…d6dbb`, `ok=true`, host `smash-tracker-f97b7.firebaseio.com`, 0 conflicts, all 12 per-tree counts equal to the Task 2.6 frozen baseline. **THREE THINGS ARE OPEN, and two of them change what step 10 means:**
  1. **Step 10 owner sign-off** — no sign-off artifact exists anywhere and no archive has run. Blocks **ACCT-02**. `30.1-ARCHIVE-DECISION.md` frames it as an explicit binary (archive after sign-off, OR record deliberate retention) — doing nothing is not one of the two states.
  2. **Gate 6 owner spot-checks against start.gg** — blocks **WKSP-01A** and VAL-01's destination half.
  3. **NEW DEFECT CLAIM, independently confirmed against source, in no prior phase artifact:** the "none of the four is a managed client of the developer's coaching account" clause (in BOTH ACCT-01 and ACCT-03) has **no executed check**. `preflightDestinations` (`manifest.ts:646`) gates on `readSubjectKind === 'ordinary'`, but `subjectKind.ts` returns `isResearchKind(parsed.data) ? 'research' : 'ordinary'` — only `research` maps away, so a **`coaching`-kind client tenant reads as `'ordinary'` and passes preflight**. That clause currently rests on owner sign-up procedure alone, not on an enforced invariant.

- **⚠️ TWO OPERATIONAL WARNINGS BEFORE ACTING ON STEP 10 — "just run archive" is NOT safe as written.** Both confirmed directly against source, not inferred:
  - **Registry drift, 7 → 15 copy trees.** `TREE_DESCRIPTORS` now carries **15** `copy` trees; the 2026-08-11 manifest covers **7** — Phases 30.2 and 30.3 added **8** more (the enrichment family plus the Gate-5 YouTube candidate tree). `migrateDemoAccounts.ts:384` runs a live `verifyMigration` re-check over the **current** registry immediately before `archiveSources` at `:422`. So once 30.2 Gate 2 lands enrichment on a destination uid while the sources stay frozen at zero, `compareTree` reports `extraIds` and **archive will correctly refuse**. Sequence the step-10 decision against this rather than discovering it at the prompt.
  - **`apps/api/migration-manifest.json` is untracked AND not gitignored** (verified: `git check-ignore` no match, `git ls-files` no match). It is the only proof of the clean copy and the only input `archive` accepts. A `git clean -fdx` would foreclose the archive option entirely, short of a re-copy.

- Migration source (complete, frozen, and copied; NOT YET ARCHIVED): research tenants `0f0443e4…` (hbox/1004), `d4c17fdc…` (mkleo/222927), `e5d9f25d…` (sparg0/158026), `1438981f…` (izaw/38222).
- Env for step 6: `DEMO_ACCOUNT_UIDS` (the four new uids) for RTEN-03; `RESEARCH_ADMIN_UIDS` must include the developer uid so `archiveClient`'s research gate passes.
- v2.5 RPT-04 soak readout (≈2026-08-14) + activation-gate flip proceed in parallel, untouched by v3.0.
- Phase 30 source evidence gate is complete (`30-UAT.md`: 7/7 passed). Final VAL-01/WKSP-01A remain open only for the authoritative Phase 30.1 post-migration destination-account check.
